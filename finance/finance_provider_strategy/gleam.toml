name = "finance_provider_strategy"
version = "0.1.0"
target = "javascript"
description = "Track-isolated provider priority, fallback, and provenance strategy"

licences = ["Apache-2.0"]
gleam = ">= 1.18.0"

[dependencies]
gleam_stdlib = ">= 0.69.0 and < 2.0.0"
finance_core = { path = "../finance_core" }
finance_http = { path = "../finance_http" }
finance_track = { path = "../finance_track" }

[dev-dependencies]
gleeunit = ">= 1.9.0 and < 2.0.0"

[javascript]
runtime = "bun"

[metadata.finance]
status = "experimental"
